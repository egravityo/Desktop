//809*??=800*??+9*??  
//其中??代表一个俩位数,并且809*??为四位数,8*??结果为俩位数,9*??
//的结果为3位数,求??代表的俩位数,及809*??的结果
#include<stdio.h>
int main()
{
	int i,j; 
	for(i=10;i<100;i++)
	{
		
		//if((809*i==800*i+9*i)&&(809*i==fun(4))&&
		//(8*i==fun(2))&&(9*i==fun(3)))
		if((809*i==800*i+9*i)&&((809*i<10000)&&(809*i>=1000))
		&&((8*i<100)&&(8*i>=10))&&((9*i<1000)&&(9*i>=100)))
		{
		    printf("??=%d,809*??=%d",i,i*809);
		}
	 } 
	return 0;
 } 
