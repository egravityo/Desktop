//书上例题连接俩字符串 
#include<stdio.h>
int main()
{
	char arr1[30],arr2[30];
	gets(arr1);
    gets(arr2);
    int i=0;
    int j=0;
    if(arr1[i]!='\0')
    {
    	i++;
	}
	if(arr2[j]!='\0')
	{
		arr1[i++]=arr2[j];
	}
	arr1[i]='\0';
	puts(arr1);
    return 0;
} 
